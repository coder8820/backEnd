<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title: 'Book Home' }) %>

  <body class="bg-gray-100 text-gray-800 font-sans">
    <%- include('../partials/navbar') %>

    <main class="max-w-4xl mx-auto px-6 py-12">
      <!-- Card -->
      <div class="bg-white rounded-xl shadow p-8">
        <!-- Home Info -->
        <h2 class="text-2xl font-bold mb-2"><%= home.housename %></h2>
        <p class="text-gray-500 mb-4">📍 <%= home.location %></p>

        <img
          src="<%= home.imageurl %>"
          class="w-full h-64 object-cover rounded-lg mb-6"
          onerror="this.src='/images/home.jpg'"
        />

        <p class="text-gray-700 mb-6"><%= home.description %></p>

        <!-- Booking Form -->
        <form action="/book" method="POST" class="space-y-5">
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div>
              <label class="block text-sm font-medium mb-1">Check-in</label>
              <input
                type="date"
                required
                class="w-full border rounded-lg px-4 py-2 focus:ring-2 focus:ring-rose-400 outline-none"
              />
            </div>

            <div>
              <label class="block text-sm font-medium mb-1">Check-out</label>
              <input
                type="date"
                required
                class="w-full border rounded-lg px-4 py-2 focus:ring-2 focus:ring-rose-400 outline-none"
              />
            </div>
          </div>

          <div>
            <label class="block text-sm font-medium mb-1">Guests</label>
            <input
              type="number"
              min="1"
              value=""
              placeholder="min(1-5)"
              class="w-full border rounded-lg px-4 py-2 focus:ring-2 focus:ring-rose-400 outline-none"
            />
          </div>

          <!-- Price -->
          <p class="text-xl font-bold text-rose-500">
            $ <%= home.price %> / night
          </p>

          <!-- Submit -->
          <button
            class="w-full bg-rose-500 text-white py-3 rounded-lg hover:bg-rose-600 transition font-semibold"
          >
            Confirm Booking
          </button>
        </form>
      </div>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>
